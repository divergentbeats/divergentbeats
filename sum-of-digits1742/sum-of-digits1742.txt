class Solution {
  public:
    int sumOfDigits(int n) {
        int s = n, sum  = 0;
        while(s>0)
        {
            int x = s % 10;
            sum += x;
            s/=10;
        }
        return sum;
        
    }
};